<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/surface"
    android:orientation="vertical"
    android:padding="@dimen/spacing_lg"
    tools:context=".ui.deck.DeckFragment">

    <!-- Parchment container -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:background="@drawable/bg_parchment"
        android:elevation="@dimen/elevation_active"
        android:orientation="vertical"
        android:padding="@dimen/spacing_xl">

        <!-- Title -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:fontFamily="@font/domine_bold"
                android:text="@string/deck_builder_title"
                android:textColor="#4a3520"
                android:textSize="@dimen/text_headline_md" />

            <TextView
                android:id="@+id/tv_deck_count"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/space_grotesk"
                android:textColor="#6b5a40"
                android:textSize="@dimen/text_label_lg"
                tools:text="6/8 cartas" />
        </LinearLayout>

        <!-- Faction balance -->
        <LinearLayout
            android:id="@+id/faction_balance_container"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_md"
            android:orientation="horizontal" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="@dimen/spacing_lg"
            android:layout_marginBottom="@dimen/spacing_lg"
            android:background="@color/outline_variant" />

        <!-- Current deck section -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/domine_bold"
            android:text="@string/deck_builder_current"
            android:textColor="#4a3520"
            android:textSize="@dimen/text_body_lg" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_current_deck"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="@dimen/spacing_sm"
            android:clipToPadding="false"
            android:paddingBottom="@dimen/spacing_sm" />

        <!-- Divider -->
        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="@dimen/spacing_sm"
            android:layout_marginBottom="@dimen/spacing_sm"
            android:background="@color/outline_variant" />

        <!-- Available cards section -->
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/domine_bold"
            android:text="@string/deck_builder_available"
            android:textColor="#4a3520"
            android:textSize="@dimen/text_body_lg" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/rv_available_cards"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:layout_marginTop="@dimen/spacing_sm"
            android:clipToPadding="false"
            android:paddingBottom="@dimen/spacing_lg" />

    </LinearLayout>

</LinearLayout>
